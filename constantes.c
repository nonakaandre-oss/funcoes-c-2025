#include <stdio.h>

#define TAMANHO 10 /*CONSTANTES são escritas em maisculo normalmente*/
                   /*É usado para valores que não sofreram alteração*/
const int MAX_SIZE = 50;  

   /*Define o tamanho maximo, porem só utiliza o necessário na compilação*/ 
   /*Constantes são definida no começo da programação*/  

int main () {
    char palavra[TAMANHO];
    char palavra_2[MAX_SIZE];

    printf("digite seu nome inteiro");
    scanf("%s", seu nome é);


    return 0;
}